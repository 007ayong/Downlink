# Provider Icons

这个目录用于放置弹窗头部的下载器图标资源。

当前代码约定的文件名如下：

- `abdownload.png`
- `motrixnext.png`
- `neatdm.png`

说明：

- 默认状态和 `aria2` 当前仍使用扩展自身图标：`icons/icon48.png`
- 如果这里的图标文件不存在，弹窗会自动回退到默认图标
- 头部实际显示尺寸是 `26x26`
- 推荐提供至少 `64x64` 或 `128x128` 的 PNG，再由界面缩放显示
- 图标应尽量保持主体清晰，避免在小尺寸下塞入过多文字
